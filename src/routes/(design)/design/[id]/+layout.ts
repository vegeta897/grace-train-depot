import type { LayoutLoad } from './$types'

// export const load = (async (event) => {
// 	console.log('design layout universal load')
// 	if (event.params.id === 'local') return { local: true }
// 	return await event.parent()
// }) satisfies LayoutLoad

/*

Go to /design/new for any new car, authed or not

Don't create a car on server until explicitly saved



*/
